doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(
            name="viewport"
            content="width=device-width, initial-scale=1.0"
        )

        title RMS Media Player

        // Font Awesome
        link(
            rel="stylesheet"
            href="/plugins/fontawesome-free/css/all.min.css"
        )

        // RMS Media CSS
        link(
            rel="stylesheet"
            href="/css/rms-media.css"
        )

    body.rms-media-body

        block content

        // RMS Media JavaScript
        script(src="/js/rms-media.js")